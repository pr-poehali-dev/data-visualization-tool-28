{
  "tests": [
    {
      "name": "Stats without token returns 403",
      "method": "GET",
      "path": "/stats",
      "expectedStatus": 403,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}
